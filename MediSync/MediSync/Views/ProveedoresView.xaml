<UserControl x:Class="MediSync.Views.ProveedoresView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <Grid Background="White" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Barra superior con búsqueda y acciones -->
        <StackPanel Orientation="Horizontal" Margin="10" VerticalAlignment="Top">
            <!-- Búsqueda -->
            <Grid Width="300" Height="30">
                <TextBox x:Name="BuscarTextBox"
                         VerticalContentAlignment="Center"
                         />
            </Grid>

            <Button Content="🔍" Width="30" Height="30" Margin="5,0,10,0"/>

            <!-- Botones de acción -->
            <Button Content="Agregar" Width="80" Height="30" Margin="0,0,5,0" Background="#2E5A88" Foreground="White"/>
            <Button Content="Editar" Width="80" Height="30" Margin="0,0,5,0" Background="#2E5A88" Foreground="White"/>
            <Button Content="Eliminar" Width="80" Height="30" Background="#8B0000" Foreground="White"/>
        </StackPanel>

        <!-- Lista de proveedores -->
        <ListView x:Name="ProveedoresListView" Grid.Row="1" Margin="10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Nombre" DisplayMemberBinding="{Binding Nombre}" Width="150"/>
                    <GridViewColumn Header="Contacto" DisplayMemberBinding="{Binding Contacto}" Width="150"/>
                    <GridViewColumn Header="Teléfono" DisplayMemberBinding="{Binding Telefono}" Width="100"/>
                    <GridViewColumn Header="Dirección" DisplayMemberBinding="{Binding Direccion}" Width="200"/>
                    <GridViewColumn Header="Email" DisplayMemberBinding="{Binding Email}" Width="150"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>

